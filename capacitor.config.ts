import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.locatr.app',
  appName: 'locatr',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
